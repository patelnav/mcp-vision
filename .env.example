# Choose one backend provider
GEMINI_PROVIDER=ais  # or "vertex"

# AI Studio (if using GEMINI_PROVIDER=ais)
GEMINI_API_KEY=your_api_key_here

# Vertex AI (if using GEMINI_PROVIDER=vertex)
# GOOGLE_CLOUD_PROJECT=your-project-id
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/key.json
# GEMINI_LOCATION=us-central1

# Model (optional, defaults to models/gemini-flash-lite-latest)
# GEMINI_MODEL=models/gemini-flash-lite-latest

# Optional: max image dimension for auto-resize (helps stay under 20MB inline limit)
# VISION_MAX_LONG_EDGE=2048
